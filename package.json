{
  "name": "wikigdd",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @wikigdd/api dev",
    "dev:web": "pnpm --filter @wikigdd/web dev",
    "build": "pnpm -r build",
    "type-check": "pnpm -r type-check",
    "db:migrate": "pnpm --filter @wikigdd/api db:migrate",
    "db:seed": "pnpm --filter @wikigdd/api db:seed",
    "db:studio": "pnpm --filter @wikigdd/api db:studio"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
